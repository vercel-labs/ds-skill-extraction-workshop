# Foundations: Index (crawl root)

## What this covers

- Crawl root for Primer's Foundations section — an index page linking the five extracted sub-pages; it carries no extractable token rules of its own.

This page (`https://primer.style/product/getting-started/foundations`) is the navigation index that Phase 1's depth-1 crawl expanded into the sub-pages extracted as sibling files: [colors](./colors.md), [icons](./icons.md), [spacing-layout](./spacing-layout.md), [responsive](./responsive.md), [typography](./typography.md). The `content` sub-page was routed out at Phase 1 as a sibling-copy-skill candidate per the scope guardrail.

Zero rules extracted here is the expected state for a crawl root that is purely an index — the rule mass lives in the sub-pages above.
