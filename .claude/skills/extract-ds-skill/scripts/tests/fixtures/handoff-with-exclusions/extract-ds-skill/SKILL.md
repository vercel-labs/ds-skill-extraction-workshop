---
name: extract-ds-skill
description: Test fixture — meta-skill self-mode. discovery.md handoff template carries `## Components proposed` plus the optional `## Known exclusions` section. HANDOFF_COMPLETENESS must report PASS (the optional section is a valid shape).
---

This fixture exercises the handoff-template completeness check with a populated Known exclusions section.
