---
name: extract-ds-skill
description: Test fixture — meta-skill self-mode. discovery.md handoff template has a `## Decisions` block but NO `## Components proposed` section. HANDOFF_COMPLETENESS must report FAIL (state/handoff-missing-component-shape).
---

This fixture exercises the FAIL branch where component shape is lost on resume.
