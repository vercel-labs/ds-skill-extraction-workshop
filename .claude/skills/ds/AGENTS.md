The `ds` skill teaches agents to build UI with Primer React (`@primer/react`).

Load `SKILL.md` first — it routes to `references/` files by trigger. Do not pre-load all references; load on demand per the routing table.

Source hierarchy: types (`.d.ts`) > component source > docs. Code wins on conflict.
