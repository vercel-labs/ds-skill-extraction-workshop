# Tokens — ds

`ds/tokens.json` is an intentional stub at `ds/tokens.json:3` ("_comment: Stub. The meta-skill reads tokens directly from @primer/primitives. This file exists so a future iteration can pin a curated subset without changing the consumer contract."). The runtime token source is `@primer/primitives@11.9.0` (`package.json:14`).

[VERIFY] The concrete subset of `@primer/primitives` in scope for `ds` is not pinned in this repo. For this workshop, treat tokens as out of scope for the sign-in fixture — Primer's components carry their default theming.

## Color

[VERIFY] Color tokens not pinned. Defer to `@primer/primitives` defaults at runtime.

## Space

[VERIFY] Space tokens not pinned. Defer to `@primer/primitives` defaults at runtime.

## Type

[VERIFY] Typography tokens not pinned. Defer to `@primer/primitives` defaults at runtime.
